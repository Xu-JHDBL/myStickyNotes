# 桌面便签（StickyNotes）技术总结

> 本文档总结「桌面便签」软件的技术层面、技术路线、开发环境与实现难点，作为项目归档与后续维护参考。

## 1. 项目概述

桌面便签是一款 Windows 桌面日程管理软件：以「便签卡片」的形式管理日程，支持桌面置顶、固定到桌面（Win+D 不消失）、到期提醒（QQ/微信式弹窗 + 托盘闪烁 + 提示音 + Windows 通知中心）、透明度调节、开机启动、系统托盘常驻与搜索过滤。

技术定位：**Electron 桌面应用**，纯前端 + Node.js 主进程，无后端服务、无数据库，数据本地 JSON 持久化。

## 2. 技术栈与编程语言

| 层面 | 技术 | 说明 |
|------|------|------|
| 编程语言 | **JavaScript (Node.js)**、**HTML5**、**CSS3** | 全项目唯一编程语言（主进程 + 渲染进程均用 JS） |
| 桌面框架 | **Electron 33** | 跨平台桌面壳，主进程 / 渲染进程 / 预加载三层架构 |
| 打包工具 | **electron-builder 25** | 产出便携版（portable）与安装版（NSIS） |
| 辅助脚本 | **PowerShell + C#**（COM interop / P/Invoke） | 创建 .lnk 快捷方式、注册 AppUserModelID |
| 数据存储 | 本地 **JSON 文件** | `notes.json` / `config.json`，`fs` 同步读写 |
| 版本/依赖 | npm + package.json | 依赖极少，仅 `electron`、`electron-builder`（均 devDependency） |

无第三方 UI 框架、无状态管理库、无数据库、无构建前端工具链（Vite/Webpack 等），渲染层为原生 DOM + CSS 毛玻璃卡片风格。

## 3. 开发与运行环境

- **操作系统**：Windows 11 Home China（10.0.26200）
- **运行时**：Node.js / npm，Electron 33.4.x
- **镜像源**：`.npmrc` 配置 `electron_mirror=https://npmmirror.com/mirrors/electron/`，规避国内网络无法访问 GitHub 的问题
- **本地离线依赖**：
  - `nsis-bundle/` — NSIS 3.12 离线构建工具（`ELECTRON_BUILDER_NSIS_DIR` 指定）
  - `scripts/setup-wincodesign.js` — 预下载 winCodeSign（rcedit）到 electron-builder 缓存
- **开发命令**：`npm install` / `npm start`（`electron .`）
- **打包命令**：`npm run build`（便携版）/ `npm run build:installer`（便携版 + 安装版）

## 4. 系统架构

```
┌──────────────────────────── Electron 应用 ────────────────────────────┐
│                                                                       │
│  主进程 (main.js)                                                      │
│  ├─ 窗口管理：主窗口(无边框透明) + 提醒弹窗(toast)                        │
│  ├─ 系统托盘 + 右键菜单 + 图标闪烁                                       │
│  ├─ 数据持久化：notes.json / config.json（JSON 读写）                    │
│  ├─ 提醒调度：30s 轮询 + notifiedSet 去重                               │
│  ├─ 开机启动：写 Startup 文件夹 .lnk                                    │
│  ├─ 通知中心：注册 AUMID 快捷方式 + new Notification()                  │
│  └─ IPC：ipcMain.handle / ipcMain.on                                   │
│         ▲  contextBridge（contextIsolation: true）                     │
│  预加载 (preload.js / preload-toast.js)                                │
│         ▲  ipcRenderer.invoke / on                                     │
│  渲染进程 (renderer/)                                                  │
│  ├─ index.html + renderer.js — 主界面（CRUD/搜索/排序/渲染/提示音）       │
│  └─ toast.html — 右下角提醒弹窗                                          │
└───────────────────────────────────────────────────────────────────────┘
```

### 4.1 三层架构与安全模型

- **主进程**：`main.js`（约 650 行），拥有完整 Node 与系统能力。
- **预加载脚本**：`preload.js`、`preload-toast.js`，通过 `contextBridge.exposeInMainWorld` 暴露白名单 API（`notesAPI` / `toastAPI`），渲染进程只接触受控接口。
- **渲染进程**：`nodeIntegration: false` + `contextIsolation: true`，走 IPC 通信，避免直接暴露 Node 能力。

### 4.2 通信方式

统一采用 `ipcMain.handle(...)` / `ipcRenderer.invoke(...)` 的请求-响应模型；事件类（托盘「新建便签」、播放提示音、弹窗点击）用 `ipcRenderer.on` / `webContents.send`。

## 5. 关键技术点与实现难点

### 5.1 固定到桌面（Win+D 不消失）

普通窗口在按下 Win+D（显示桌面）时会被最小化/隐藏。方案采用「屏幕保护层级 + 轮询守护」：

- 将窗口设为 `alwaysOnTop: true, level: 'screen-saver'`（比普通置顶更高的层级）。
- 启动 500ms 的 `setInterval` 守护线程：一旦检测到窗口被隐藏且未退出，立即 `showInactive()` 恢复显示，实现「钉在桌面」效果。

难点在于 Win+D 属于系统级窗口行为，没有官方 API 直接拦截，只能靠层级 + 轮询补偿。

### 5.2 无边框透明窗口 + 毛玻璃卡片 UI

主窗口 `frame: false` + `transparent: true`，去掉系统边框后自绘标题栏（拖拽、最小化、隐藏到托盘按钮全部自定义）。配合 CSS 毛玻璃（半透明背景 + 模糊 + 阴影）卡片风格，以及透明度滑块实时调用 `mainWindow.setOpacity()`。

### 5.3 系统托盘与动态闪烁

- 托盘图标用 `nativeImage.createFromPath('assets/icon.png').resize(16×16)` 加载，与 exe 图标一致。
- **闪烁提醒**：`startTrayBlink()` 每 500ms 在正常图标与「红点角标」图标间切换。红点角标图标由**渲染进程用 canvas 动态绘制**（在图标右上角画红色圆 + 白色「1」），再通过 `nativeImage.createFromDataURL` 回传给主进程。这一「前端生成、主进程使用」的跨进程协作是托盘动态图标的常见做法。

### 5.4 QQ/微信式到期提醒（四件套）

到期提醒不依赖单一机制，而是组合四层，保证各种场景都生效：

1. **右下角弹窗**：无边框置顶小窗口 `toast.html`，用 `tray.getBounds()` 定位到托盘正上方，6 秒自动隐藏，点击打开主窗口。
2. **托盘闪烁**：见 5.3，直到用户打开窗口/点击弹窗才停止。
3. **提示音**：渲染进程用 **Web Audio API** 合成双音「叮咚」（880Hz→1320Hz 正弦波，指数包络），主窗口设 `backgroundThrottling: false` 保证隐藏时也能发声。
4. **Windows 通知中心**：`new Notification()` 写入系统通知中心（见 5.5）。

### 5.5 Windows Toast 通知中心与 AUMID 注册（核心难点）

Windows 通知中心要显示某应用的 Toast，必须存在一个带 **AppUserModelID（AUMID）** 的「开始菜单」快捷方式，否则 Toast 静默失效（这正是便携版早期通知失灵的原因）。

- **安装版**：electron-builder 根据 `appId` 自动写入带 AUMID 的快捷方式，无需处理。
- **便携版 / 开发模式**：启动时 `registerToastShortcut()` 动态注册 `%APPDATA%\...\Start Menu\Programs\桌面便签.lnk`。

创建带 AUMID 的快捷方式，`WScript.Shell` 的 `CreateShortcut` 做不到（它无法写 AUMID 属性），因此**内嵌一段 C#**（`AUMID_CSHARP`），用 COM 互操作调用：

- `IShellLinkW` 设置目标路径、参数、工作目录、图标；
- `IPropertyStore` 写入 `PKEY_AppUserModel_ID`（`fmtid = {9F4C2855-...}`，`pid = 5`，`PROPVARIANT` 存 LPWSTR）；
- `IPersistFile::Save` 落盘。

C# 通过 `Add-Type -TypeDefinition` 注入 PowerShell，由 Node 的 `execFileSync` 调用 `powershell.exe -File` 执行。这段逻辑同时复用于「开机启动」快捷方式的创建（`createShortcutSync`）。

### 5.6 开机启动

不使用 `app.setLoginItemSettings()`（开发模式下路径不稳定），改为直接向启动文件夹写入快捷方式：

```
%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\StickyNotes.lnk
```

勾选时创建，取消时删除。关键点：打包版快捷方式的 `Arguments` 应为**空字符串**（exe 无需参数），开发版才传 `__dirname`；工作目录/图标按 `app.isPackaged` 分别取 exe 所在目录与 exe 自身。

### 5.7 Unicode 路径处理（曾经踩坑）

应用安装路径与用户目录可能含中文（如 `D:\...\桌面便签\`）。早期方案用 **VBS + GBK 编码**（`iconv-lite`）创建快捷方式，但 `iconv-lite` 只是 devDependency，**打包后不在 app.asar 里**，导致 `require('iconv-lite')` 在打包版抛异常、开机启动静默失败。

现统一改为 **PowerShell 脚本 + UTF-16LE（带 BOM）**：PowerShell 原生支持 Unicode 路径，脚本文件以 `fs.writeFileSync(psPath, '\ufeff' + ps, 'utf16le')` 写出，彻底摆脱对 `iconv-lite` 的依赖。

### 5.8 图标嵌入 exe（winCodeSign / rcedit）

exe 自身图标、快捷方式图标、任务栏图标由 `signAndEditExecutable: true` 触发 rcedit 写入。两个坑：

- rcedit 来自 electron-builder 的 `winCodeSign` 工具包，默认从 GitHub 下载（国内不可达）；
- 压缩包内含 macOS 符号链接（`darwin/*.dylib`），Windows 无管理员权限时 7za 解压会因 `SeCreateSymbolicLinkPrivilege` 失败。

解决：`scripts/setup-wincodesign.js` 在打包前从 npmmirror 镜像下载，并以「排除 darwin/linux」的方式解压到 electron-builder 缓存，命中缓存后跳过下载解压。图标本身（`icon.ico`）为 7 尺寸（16~256）方形 ICO。

### 5.9 app.asar 只读约束

打包后代码运行在只读的 `app.asar` 内，`__dirname` 指向 asar 内部，**不能往里写文件**。所有临时文件（`.ps1` 脚本）一律写入 `app.getPath('temp')`；Windows 也无法读取 asar 内的图标路径，故打包版图标一律取 exe 自身。

### 5.10 单实例锁

`app.requestSingleInstanceLock()` 防止多开；二次启动时通过 `second-instance` 事件把已有窗口唤起，避免数据并发写。

## 6. 数据持久化

- 路径：`app.getPath('userData')` = `%APPDATA%\desktop-sticky-notes\`（注意是包名 `desktop-sticky-notes`，而非中文产品名「桌面便签」）。
- `notes.json`：便签数组（标题、内容、日期、时间、优先级、提醒开关、完成状态、时间戳）。
- `config.json`：`opacity`、`pinnedToDesktop`、`autoStart` 等设置。
- 均用 `fs.readFileSync` / `writeFileSync` 同步读写，数据量小、无需数据库。

## 7. 打包与分发

| 目标 | 命令 | 产物 | 特点 |
|------|------|------|------|
| 便携版 | `npm run build` | `桌面便签_便携版.exe` | 免安装，直接运行 |
| 安装版 | `npm run build:installer` | `桌面便签_安装版.exe` | NSIS 向导，可自定义安装目录 |

关键打包配置：`appId: com.stickynotes.desktop`、`productName: 桌面便签`、`signAndEditExecutable: true`（嵌入图标/版本）、NSIS `oneClick: false` + 创建桌面/开始菜单快捷方式。

未配置数字证书，electron-builder 自动跳过签名；分发时 Windows SmartScreen 会提示「仍要运行」。

## 8. 开发过程中踩过的坑（小结）

| 坑 | 现象 | 解决 |
|----|------|------|
| `signAndEditExecutable: false` | exe/快捷方式/任务栏显示 Electron 默认图标 | 改为 `true`，并补齐 rcedit |
| winCodeSign 下载失败 / 符号链接解压失败 | 打包中止 | npmmirror 镜像 + 排除符号链接解压的 `setup-wincodesign.js` |
| `iconv-lite` 打包后缺失 | 开机启动静默失败 | 改用 PowerShell + UTF-16LE 脚本 |
| 便携版 Toast 失效 | 通知不进通知中心 | 动态注册带 AUMID 的开始菜单快捷方式 |
| 往 asar 内写临时文件 | 报错 / 失效 | 改写 `app.getPath('temp')` |
| Windows 读取 asar 内图标 | 快捷方式图标空白 | 打包版图标取 exe 自身 |
| 开机启动快捷方式带 asar 参数 | 无法正常启动 | 打包版 `Arguments` 置空 |

## 9. 未来可改进方向

- 数据存储从 JSON 迁移到 SQLite，支持更大数据量与并发。
- 提醒调度当前为 30s 轮询，可改为按最近到期时间动态定时。
- 引入前端框架 / 构建工具链（Vite + Vue/React），提升可维护性。
- 补充代码签名证书，消除 SmartScreen 警告。
- 增加多语言支持与主题切换。
