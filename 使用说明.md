# 桌面便签 · 使用说明

## 安装

- **安装版**：双击 `桌面便签_安装版.exe`，按提示安装即可。
- **便携版**：双击 `桌面便签_便携版.exe` 直接运行，无需安装。

> 首次运行如遇 Windows 安全提示，点击「更多信息」→「仍要运行」。

## 基本操作

| 操作 | 方法 |
|------|------|
| 新建便签 | 点右上角 `＋`，或按 `Ctrl + N` |
| 编辑便签 | 点击便签卡片 |
| 标记完成 | 点便签左侧圆圈 |
| 删除便签 | 鼠标移到卡片上，点 `✕` |
| 搜索 | 在搜索栏输入关键词 |

## 便签字段

- **标题**：必填，日程名称
- **内容**：可选，补充说明
- **日期 / 时间**：日程时间，用于提醒
- **优先级**：🔴高 / 🟡中 / 🟢低，卡片左侧颜色条区分
- **提醒**：开启后到期弹出通知

## 窗口控制

| 按钮 | 功能 |
|------|------|
| `🖥️` | 固定到桌面（绿色=已固定，按 Win+D 不消失） |
| `📌` | 窗口置顶（始终在其它窗口之上） |
| `＋` | 新建便签 |
| `─` | 最小化到任务栏 |
| `✕` | 缩到系统托盘（不退出） |

- **透明度**：标题栏下方滑块，左右拖动调整。
- **退出**：右键系统托盘图标 → 退出。

## 系统托盘

右下角托盘图标，右键菜单：

- 显示 / 隐藏便签
- 新建便签
- **开机启动**（勾选后开机自动运行）
- 退出

## 提醒说明

到期时像 QQ / 微信一样提醒你：

- **右下角弹窗**：弹出便签卡片，6 秒后自动消失，点击可打开软件。
- **托盘图标闪烁**：右下角图标持续闪烁，直到你打开窗口或点击弹窗。
- **提示音**：播放一段「叮咚」提示音。
- **通知中心**：同时写入 Windows 通知中心（便携版首次运行会自动注册开始菜单快捷方式，之后即生效）。
- 过期的未完成日程会显示 ⚠️ 标记。

## 数据位置

便签数据自动保存在本机 `%APPDATA%\desktop-sticky-notes\` 目录下：

- `notes.json` — 便签数据
- `config.json` — 设置（透明度、置顶、开机启动等）

卸载前可自行备份该目录。
